---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<header class="flex ms-8 text-3xl md:text-4xl font-semibold items-center gap-2">
  <slot name="icon" />
  <h1>{title}</h1>
</header>
